{
  "private": true,
  "name": "@turbo-vite/typescript-config",
  "version": "0.0.0",
  "scripts": {
    "format": "prettier . --check --ignore-path ../../.prettierignore --loglevel=warn",
    "format:fix": "prettier . --write --ignore-path ../../.prettierignore"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
